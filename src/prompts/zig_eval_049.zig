const std = @import("std");

/// Checks if a given string reads the same forwards and backwards (case-sensitive check).
pub fn is_palindrome() !void {
    // TODO: Implement is_palindrome
    @compileError("Not implemented");
}

test "is_palindrome" {
    // TODO: Add tests
}
