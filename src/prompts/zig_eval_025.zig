const std = @import("std");

/// Finds the largest divisor of n that is smaller than n (for n > 1).
pub fn largest_divisor() !void {
    // TODO: Implement largest_divisor
    @compileError("Not implemented");
}

test "largest_divisor" {
    // TODO: Add tests
}
